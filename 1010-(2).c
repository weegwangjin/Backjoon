#include <stdio.h>

int main_C()
{
	int matrix[31][31];

	int i, j, k;
	int test_case, a, b;
	int temp;
	int result;
	scanf_s("%d", &test_case);

	for (k = 0; k < test_case; k++) {
		scanf_s("%d %d", &a, &b);

	}
}